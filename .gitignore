__pycache__/
*.pyc

# Stockfish binary (114MB, over GitHub's 100MB limit — re-downloadable, see README)
engine/

# local config overrides (see .env.example)
.env
flask.log

# personal game history / trained checkpoints
data/history.db
data/*.pgn
data/*.jsonl

# personal style-model checkpoint (username-named) — only the generic
# demo_policy.pt is meant to be public
models/*_policy.pt
!models/demo_policy.pt
